version: "3.8"
services:
  news-prediction-be:
    build: .
    restart: always
    container_name: "news-prediction-be"
    ports:
      - "5000:5000"

  news-prediction-fe:
    build: ./src/client/news-prediction-app
    restart: always
    container_name: "news-prediction-fe"
    ports:
      - "3000:3000"
    depends_on:
      - news-prediction-be
